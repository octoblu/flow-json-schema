{
	"title": "Debounce",
	"type": "object",
	"properties": {
		"timeout": {
			"type": "integer",
			"title": "Timeout"
    },
    "unit": {
			"type": "string",
			"title": "Units",
			"enum": [
				"milliseconds",
				"seconds",
				"minutes",
        "hours"
			]
		}
	}
}
