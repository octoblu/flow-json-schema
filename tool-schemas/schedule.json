{
	"title": "Schedule",
	"type": "object",
	"properties": {
		"crontab": {
      "type": "string",
      "title": "Schedule (crontab, Flows run in the UTC timezone)"
    },
		"payloadType": {
			"type": "string",
			"title": "Payload Type",
			"enum": [
				"date",
				"none",
				"string"
			]
		},
    "payload": {
			"type": "string",
			"title": "Payload"
		},
		"topic": {
			"type": "string",
			"title": "Topic"
		}
	}
}
